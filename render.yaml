services:
  - type: web
    name: funeral-home-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: sqlite-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATABASE_PATH
        value: /data/funeral_home.db
